[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nju-agent"
version = "1.0.0"
dependencies = ["openai>=2.0.0", "rich>=13.7.0", "tiktoken>=0.7.0"]
